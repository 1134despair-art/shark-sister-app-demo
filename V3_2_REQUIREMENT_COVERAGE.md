# 鲨鱼妹妹 V3.2 严格需求覆盖报告

审计依据为 `鲨鱼妹妹项目需求-APP端功能列表_V3.2.docx` 原始表格第 004-146 行、当前 UniApp 源码和自动化测试。第 008-146 行共 139 行，其中 6 行是原始空行；第 004-006 行单独记录版本、认证方式和数据驻留约束。

本报告采用严格口径：存在页面、路由或点击反馈不等于完成。只有字段、校验、状态流转、关联数据持久化和测试证据一致时，才标记为 `complete-local`。

UI 设计库当前共有 115 页，其中 105 页与本报告的严格验收状态一一对应；另外 5 页为派生状态、5 页为保留的扩展设计。扩展页不改变本报告的 V3.2 完成数量，逐项关系见 `../ui-design/V3_2_UI_COVERAGE.md`。

## 当前覆盖状态

| 状态 | 数量 | 判定规则 |
|---|---:|---|
| `complete-local` | 80 | 纯前端范围内已形成数据闭环，且与需求语义一致（含第 004 行平台版本标识） |
| `integration-required` | 2 | 第 005、006 行的生产数据驻留、服务器和 CDN 约束 |
| `simulated-local` | 32 | 依赖硬件或第三方能力，但本地适配器能完整驱动等价状态流转 |
| `partial` | 21 | 已有页面或部分逻辑，但字段、流程或角色语义仍与 V3.2 有偏差 |
| `demo-exception` | 1 | 第 136 行平台审批，本地执行前必须明确提示并记录审计来源 |
| `missing` | 6 | DOCX 第 059、060、100、110、111、112 行为空行，不虚报功能 |

第 004 行为本地完成。第 005、006 行的认证界面可本地运行，但国内/海外服务器数据驻留和 CDN 属于 `integration-required`。

## 已确认的 21 条偏差

| 行号 | 需求主题 | 当前偏差 | 正确整改方向 |
|---:|---|---|---|
| 016 | 微信注册绑定手机号 | 微信入口直接形成账号，未强制经过手机号绑定 | 微信授权后进入手机号验证码绑定，再创建账号 |
| 053-054 | 定位权限与销售区域 | 仅切换布尔状态并使用固定地区 | 本地模拟应产生明确坐标和地区结果，并可触发授权拒绝状态 |
| 055-057 | 地图定位与服务选择 | 当前主要是静态底图，定位结果未驱动地图 | 用本地 MapAdapter 驱动中心点、标记和服务能力状态 |
| 065 | 项目不存在时阻断 | 合法格式的任意 SN 会被自动补建项目和登记 | 只允许命中预置登记结果；未建项目必须显示阻断与经销商入口 |
| 073 | 遥控参数与状态 | 控制页只展示简化模式、功率和设备名 | 按设备类型显示需求中的遥控状态和参数 |
| 078 | 后台参数设置 | 当前设置项和设备类型差异不足 | 使用与遥控器一致的参数模型，但不发送实时控制指令 |
| 079 | 保存当前位置为航点 | 入口打开空表单，未带入当前位置 | 自动带入当前坐标，用户只补名称和说明 |
| 081 | 航点地图展示 | 标记主要为静态视觉占位 | 根据已保存航点数据渲染真实本地标记 |
| 087 | 航点导航 | 仅播放进度动画，没有导航任务状态 | 创建本地导航任务并更新设备、航线、时间线和审计 |
| 093-094 | OTA 自动检测与提醒 | 只有开关和手动升级页 | App 启动/设备连接时检测，出现“立即/稍后”弹窗 |
| 119 | 申请与发货状态 | 一个状态字段混合表达申请和物流阶段 | 分开显示申请状态、物流状态和各自时间线 |
| 132 | 一级向二级经销商分配设备 | 当前 `assignment` 实现为分配给员工 | 改为选择下级经销商并更新设备经销商归属 |
| 133-134 | 分配记录与售后归属 | 记录和归属建立在员工分配语义上 | 记录上下级经销商分配，售后按新 `dealerId` 隔离 |
| 135 | 向其他经销商调货 | 候选范围只覆盖部分下级经销商 | 根据需求提供可调货经销商并校验区域及归属 |
| 144 | 投诉关联订单/SN | 当前强制同时填写订单和 SN，需求为可选关联 | 两项均可选；填写时才校验归属和匹配关系 |
| 145 | 跨区域售后 | 在原经销商确认和目标经销商接收后额外增加平台审批 | 严格收敛为需求规定的两方确认流程 |

## 不属于缺口的本地模拟

蓝牙扫描、扫码、设备控制、OTA 传输、支付、推送、物流查询和第三方认证在 Demo 阶段可以使用本地适配器。验收条件不是连接真实 SDK，而是：输入可继续、状态机合法、关联实体同步、刷新后保留、失败可重试、审计可追踪。

手工输入采用宽松但可解释的规则：允许常见 SN 格式和预置的可激活/已绑定/跨区/未建项目样例；不允许任意输入绕过 V3.2 的项目、区域、归属和重复绑定校验。

## 已对齐的主要模块

- 手机号/邮箱注册、验证码、找回密码、首次改密和账号锁定。
- 头像、昵称、账号安全、退出登录、语言和通知设置。
- 首页 Banner、横向最近设备、设备状态和入口布局。
- 设备详情、绑定异常分支、解绑、主板更换时长继承。
- 航点与航线 CRUD、排序、离线队列和失败重试。
- 留言、报修、附件、工单归属、留言转单和处理时间线。
- 项目分步建档、只搜索不浏览、员工权限、物料审批、库存和物流签收。
- 经销商统计、价格权限、支付记录和第 146 行留言升级转单。

## 验收限制

`src/config/requirements.ts` 是逐行审计登记，不再把路由存在当作完成功能。测试必须同时断言页面状态、本地实体、关联实体、时间线和审计记录；生产数据驻留、真实资金扣款和真实硬件动作不能由静态前端证明完成。
